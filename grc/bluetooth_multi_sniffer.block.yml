# auto-generated by grc.converter

id: bluetooth_multi_sniffer
label: Bluetooth Multi Sniffer
category: '[Bluetooth]'

parameters:
-   id: sample_rate
    label: Sample Rate
    dtype: int 
    default: samp_rate
-   id: center_freq
    label: Center Frequency
    dtype: int 
    default: '2476000000'
-   id: squelch_threshold
    label: Squelch Threshold
    dtype: int 
    default: '10'
-   id: tun
    label: TUN Interface 
    dtype: bool
    default: 'false'

inputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gr_bluetooth
    make: gr_bluetooth.multi_sniffer(${sample_rate}, ${center_freq}, ${squelch_threshold}, ${tun})

file_format: 1
